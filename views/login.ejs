
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Technician Finder Login Page</title>
    <link rel="stylesheet" href="../CSS/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="header-div">
            <h1>Welcome to Technician Finder</h1>
        </div>
        <div class="form-container">
            <h2>Login</h2>
            <form action="/login" method="POST">
                <!-- <input type="text" name="username" placeholder="Username" required> -->
                <!-- <input type="tel" name="username" pattern="[0-9]{10}" maxlength="10" placeholder="Username" required> -->
                <input type="tel" name="username" pattern="[0-9]{10}" maxlength="10" placeholder="Username" required onkeypress="return isNumberKey(event)">
                <script>
                    function isNumberKey(event) {
                        var charCode = (event.which) ? event.which : event.keyCode;
                        if (charCode > 31 && (charCode < 48 || charCode > 57)) {
                            event.preventDefault();
                            return false;
                        }
                        return true;
                    }
                </script>

                <input type="password" name="password" placeholder="Password" required>

                <!-- <label for="role">Role:</label>
                <select id="roe" name="role" multiple required>
                    <option value="client">Client</option>
                    <option value="technician">Technician</option>
                    <!-- Add more options as needed -->
                </select>
                <button type="submit">Login</button>
            </form>
        </div>
        <div class="form-container">
            <h2>Signup</h2>
            <p>Are you a technician or a client?</p>
            <a href="/signupt" class="signup-btn">Signup as Technician</a>
            <a href="/signupc" class="signup-btn">Signup as Client</a>
        </div>
    </div>
</body>
</html>


